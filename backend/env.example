## Backend (Express)
# Puerto del servidor (si no se provee, usa el default configurado en src/config.ts)
PORT=3001

# Base URL pública del backend (para webhooks)
BACKEND_URL=http://localhost:3001

# Base URL pública del frontend (para back_urls de Mercado Pago)
FRONTEND_URL=http://localhost:3000

# Base de datos Postgres
DATABASE_URL=postgresql://USER:PASSWORD@localhost:5432/cinema_pergamino

# Redis (opcional pero recomendado para locks/cleanup)
REDIS_URL=redis://localhost:6379

# Auth
JWT_SECRET=change_me_in_production

# Mercado Pago (opcional en modo mock)
MERCADOPAGO_ACCESS_TOKEN=


